import {
    iconButton, backgroundPrimary,
} from '../styles';

const logoutStyle = {
    iconButton,
    root: {
        width: "100%",
        height: "100vh",
        overflow: "hidden",
        background: backgroundPrimary,
    },
    sideForm: {
        padding: "10px"
    }
};

export default logoutStyle;
