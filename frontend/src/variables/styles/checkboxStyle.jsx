import {
    primaryColor,
} from "../styles";

const checkboxStyle = {
    checkboxRoot: {
        color: "#fff",
        marginLeft: "auto !important",
    },
    checkboxChecked: {
        color: primaryColor + ' !important',
    },
};

export default checkboxStyle;