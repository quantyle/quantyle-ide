import {
    primaryColor,
    backgroundPrimary,
    backgroundDark,
} from '../styles';

const textColor = "#ddd"

const settingsStyle = {
    // slider
    title: {
        color: textColor,
        margin: '25px 0px 5px 0px',
    },
    icon: {
        color: primaryColor,
    },
    root: {
        color: primaryColor,
        height: 2,
        padding: '15px 0',
    },
    label: {
        color: textColor,
        marginTop: 20
    },
    range: {
        color: textColor,
        marginTop: '5px',
    },
    max: {
        float: 'right'
    },
};

export default settingsStyle;
