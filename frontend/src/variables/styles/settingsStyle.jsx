import {
    iconButton,
} from '../styles';

const settingsStyle = theme => ({
    iconButton,
    settingsRoot: {
        width: "100%",
        alignContent: 'center',
    },
    // paper: {
    //     maxWidth: "840px",
    //     margin: '30px auto',
    // }
    leftWrapper: {
        height: '100vh'
    }
});

export default settingsStyle;
